# [todo][update] updateTodoDto doesn't touch todos if tagIds is undefined, clears if set to [].

# [todo][tag][naming] tags are named todoTag because they belong to todos currently

# [prisma] production migration should happen with dotenv -e .env.production -- prisma migrate deploy

# [research] research functionality is fire-and-forget. when a research is triggered, its id is returned in the response that can be used to open a stream to research updates.
