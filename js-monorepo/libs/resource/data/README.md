# resource-data

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build resource-data` to build the library.

## Running unit tests

Run `nx test resource-data` to execute the unit tests via [Jest](https://jestjs.io).
