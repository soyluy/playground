# resource-api

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build resource-api` to build the library.

## Running unit tests

Run `nx test resource-api` to execute the unit tests via [Jest](https://jestjs.io).
