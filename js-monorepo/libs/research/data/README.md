# research-data

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build research-data` to build the library.

## Running unit tests

Run `nx test research-data` to execute the unit tests via [Jest](https://jestjs.io).
