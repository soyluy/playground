# event-data

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build event-data` to build the library.

## Running unit tests

Run `nx test event-data` to execute the unit tests via [Jest](https://jestjs.io).
